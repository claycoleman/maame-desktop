import React from 'react';

// TODO really an info page with a link to our PayPal donations
const DonationsPage = () => (
  <div>
    <h1>Donations Page!</h1>
    {/* TODO import the same page that we have in the app */}
  </div>
);

export default DonationsPage;
