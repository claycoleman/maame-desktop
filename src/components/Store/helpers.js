export function tloListenerKey(tloId) {
  return `tlo-${tloId}`
}

export function tloOrgsListenerKey(tloId) {
  return `tlo-orgs-${tloId}`
}

export function orgListenerKey(orgId) {
  return `org-${orgId}`
}